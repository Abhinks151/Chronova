<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>Simple Home Page</title>
	</head>
	<body
		style="font-family: Arial, sans-serif; background-color: #f4f4f4; margin: 0; padding: 40px; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; color: #333;"
	>
		<header style="margin-bottom: 40px;">
			<h1 style="font-size: 2.5rem; margin: 0;">
				Welcome to the Home Page
			</h1>
		</header>

		<main style="max-width: 600px; text-align: center;">
			<p
				style="font-size: 1.2rem; line-height: 1.5; margin-bottom: 20px;"
			>
				This is a simple home page for testing purposes. Nothing fancy,
				just a clean and clear layout.
			</p>

			<button
				id="logout-btn"
				style="padding: 12px 24px; font-size: 1rem; border: none; background-color: #1a1a1a; color: #fff; border-radius: 6px; cursor: pointer; transition: background-color 0.3s ease;"
			>
				Logout
			</button>
			<button
				id="forget-btn"
				style="padding: 12px 24px; font-size: 1rem; border: none; background-color: #1a1a1a; color: #fff; border-radius: 6px; cursor: pointer; transition: background-color 0.3s ease;"
			>
				forget password
			</button>
		</main>

		<footer
			style="margin-top: auto; font-size: 0.9rem; color: #666; margin-top: 40px;"
		>
			&copy; 2025 Your Company
		</footer>
		<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
		<script>
			const logoutButton = document.getElementById('logout-btn');
			const forgetButton = document.getElementById('forget-btn');

			logoutButton.addEventListener('click', ()=>{
			  axios.get('/user/logout')
          .then(response => {
			      window.location.href = '/user/login';
			    })
          .catch(error => {
            console.error(error);
          });
			})

      forgetButton.addEventListener('click', ()=>{
        axios.get('/user/forgot-password')
          .then(response => {
            window.location.href = '/user/forgot-password';
          })
          .catch(error => {
            console.error(error);
          })
        })
		</script>
	</body>
</html>
