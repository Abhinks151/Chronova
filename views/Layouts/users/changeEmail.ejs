<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Change Email</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f8f8f8;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background-color: #ffffff;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
      text-align: center;
    }

    .email-icon {
      font-size: 40px;
      margin-bottom: 15px;
    }

    h2 {
      margin: 0;
      font-size: 24px;
      color: #111;
    }

    p {
      font-size: 13px;
      color: #555;
      margin: 10px 0 25px;
    }

    input[type="email"] {
      width: 100%;
      padding: 12px;
      border-radius: 6px;
      border: 1px solid #ccc;
      margin-bottom: 16px;
      font-size: 14px;
    }

    .error-message {
      color: #d93025;
      background-color: #fce8e6;
      border: 1px solid #f5c6cb;
      padding: 10px;
      margin-bottom: 16px;
      border-radius: 6px;
      font-size: 13px;
    }

    .success-message {
      color: #0f5132;
      background-color: #d1e7dd;
      border: 1px solid #badbcc;
      padding: 10px;
      margin-bottom: 16px;
      border-radius: 6px;
      font-size: 13px;
    }

    button {
      background-color: #000;
      color: #fff;
      padding: 12px 20px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.2s ease-in-out;
    }

    button:hover {
      background-color: #222;
    }

    .back-link {
      display: block;
      margin-top: 20px;
      font-size: 13px;
      color: #000;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="email-icon">📧</div>
    <h2>Change Email</h2>
    <p>Enter your new email address below. We'll send a verification link to confirm the change.</p>

    <form action="/user/profile/changeEmialVerificationCode" method="POST">
      <input type="email" name="newEmail" placeholder="Enter new email" required />

      <% if (error) { %>
        <div class="error-message"><%= error %></div>
      <% } %>

      <% if (success) { %>
        <div class="success-message"><%= success %></div>
      <% } %>

      <button type="submit">Send Verification Code</button>
    </form>

    <a href="/user/profile" class="back-link">Back to Profile</a>
  </div>
</body>
</html>
