<% for (let i = 1; i <= totalPages; i++) { %>
            <% if (i === currentPage) { %>
              <span class="pagination-btn active"><%= i %></span>
            <% } else { %>
              <a 
                href="?page=<%= i %>&limit=<%= limit %>&status=<%= statusFilter %>&sort=<%= sort %>" 
                class="pagination-btn"
              >
                <%= i %>
              </a>
            <% } %>
          <% } %>





// created at field in DataBase

FOLDER STRUCTURE
  config
  controllers
  helpers
  middleware
  models
  public
  routes
  servises
  utils
  views
  server.js



  //product pagination limit seletctor
          <div class="page-size-group">
          <span class="page-size-label">Show:</span>
          <select id="pageSizeSelect" class="page-size-select">
            <option value="10">10</option>
            <option value="25" selected>25</option>
            <option value="50">50</option>
            <option value="100">100</option>
          </select>
        </div>
  


// check filteration is proper in product management;
// filter and sortOption in paginationService


// error handiling

// editProduct page line :: 54 
<option value="<%= brand._id %>" 
                                    <%= product.brand && product.brand.toString() === brand._id.toString() ? 'selected' : '' %>>
                                    <%= brand.name %>
                                </option>